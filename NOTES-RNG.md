Tests RNG des deux cas.

* Validation direct XML Maven Plugin avec SchemaLanguage
  * Dépendence vers le nouveau Jing
* Validation via jxvc / xml-model
   * Builder (mvn install) jxvc avec le Jing Neuf